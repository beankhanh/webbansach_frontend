const ServerAPI ="http://localhost:8080"
const BookApi = `${ServerAPI}/sach`
export default BookApi;

// require('dotenv').config();

// Sử dụng biến ServerAPI
// const ServerAPI = process.env.ServerAPI || 'http://localhost:8080/';

// // Sử dụng biến ServerAPI trong mã nguồn của bạn
// const BookApi = `${ServerAPI}/sach`;
// export default BookApi;